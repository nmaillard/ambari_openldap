# ambari_openldap
Small script to automate ambari openldap install
- intalls openldap
- populates with sample users,groups
- configures ambari
- synch ambari

To start run automate_install.sh

you will find in config.cfg the default configurations including the admin password for openldap that will be hadoop
